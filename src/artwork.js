class Artwork{
    constructor(name,height,width,grid, user_id) {
        this.name = name,
        this.height = height,
        this.width = width,
        this.grid = grid
        this.user_id = user_id
    }
}
